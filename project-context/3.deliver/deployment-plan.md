# Deployment Plan
## Recruitment Assistant

## Deployment Approach

The application will be deployed locally for the mini-project, with Docker support included for repeatable execution.

## Dependencies

- Python 3.8+
- FastAPI
- Uvicorn
- Pydantic
- python-dotenv

## Environment Configuration

Required environment variables are documented in `.env.example`.

## Local Deployment Steps

1. Install dependencies:

```bash
python3 -m pip install -r requirements.txt